# Retranslate

翻译/改写助手：识别输入语言，翻译成母语或目标语言，并在需要时给出地道改写版本。

## Language

**母语 (确认语言)**:
用户设定的固定基准语言，config 里的单值（literal），不是列表。作为翻译的最终归宿——任何非母语输入都会被翻译回母语，供用户核对语义。

**目标语言 (输出语言 list)**:
母语之外，需要输出译文/改写的语言集合，可配置多个（如英文、德文）。
_Avoid_: 输出语言（仅在 config 字段名里用，指代同一概念）

**输入语言**:
语言识别器判定出的、当前段落所使用的语言。取值范围被约束为 `{母语} ∪ 目标语言 list`——不会出现集合之外的语言。

**翻译后段落**:
`dict[语言 → 文本]`。key 是译出的目标语言：母语路径下 key 是目标语言 list 中的各语言；非母语路径下 key 是母语。
_Avoid_: 译文（笼统，未体现多语言/dict 结构）

**改写后段落**:
单一字符串。只在输入语言属于目标语言 list 时产出，改写成"输入语言"对应的地道表达；语言由输入语言隐含，不单独标注。
